import * as elements from './elements'

import Range, { Props as RangeSliderProps } from './RangeSlider'
import Value, { Props as ValueSliderProps } from './ValueSlider'

export type { RangeSliderProps, ValueSliderProps }

export default { ...elements, Range, Value }
